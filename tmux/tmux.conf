setw -g mode-mouse on                                                                       â
set -g mouse-resize-pane on                                                                 â
set -g mouse-select-pane on                                                                 â
set -g mouse-select-window on                                                               â
set -g history-limit 1000                                                                   â
set -g status-interval 1                                                                    â
set -g status-left '#H#[default]'                                                           â
set -g status-right '#(cut -d â â -f 1-4 /proc/loadavg)#[default] #%Y-%m-%d %H:%M:%S#[defaulâ
t]'                                                                                         â
setw -g monitor-activity on                                                                 â
set -g visual-activity on                                                                   â
                                                                                            â
# fix ssh agent when tmux is detached                                                       â
set -g update-environment -r                                                                â
setenv -g SSH_AUTH_SOCK $HOME/.ssh/ssh_auth_sock
